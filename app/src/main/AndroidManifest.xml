<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.shohoku.s18b706.ueno.policeman">

    <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/AppTheme">
    <activity
        android:name=".OP"
        android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:label="@string/app_name" android:name=".Kawaii"/>
        <activity android:label="@string/app_name" android:name=".BowWow"/>
        <activity android:label="@string/app_name" android:name=".Kawaii2"/>
        <activity android:label="@string/app_name" android:name=".BadEnd"/>
</application>

</manifest>